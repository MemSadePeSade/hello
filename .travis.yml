language: cpp
  script:
  - cmake .
  - cmake --build .
  - cmake --build . --target package
  deploy:
    provider:script
    skip_cleanup: true
    script:
    -curl -T helloworld-8.8.$TRAVIS_BUILD_NUMBER-Linux.deb -MemSadePeSade:$BINTRAY_API_KEY 
    "https://api.bintray.com/content/"

    :deb_distribution=trusty;deb_component=main;
    deb_architecture=amd64;publish=1"